services:
  - type: web
    name: sahaja-krushi-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: "Set this in Render dashboard"
      - key: JWT_SECRET
        value: "Set this in Render dashboard"
